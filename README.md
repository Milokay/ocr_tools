# 🔧 OCR Tools - Cable Automation System

> Production-ready OCR automation for cable price updating

## 🚀 Quick Start

```bash
# Test with working automation
python src/cable_automation_v2.py

# Test OCR functionality
python tests/test_tesseract_basic.py

4. **Commit the new README**

### **Step 4: Create Empty Folders (Optional)**

To create additional folders like `config/`:

1. **Add file → Create new file**
2. **Type**: `config/temp.txt`
3. **Add some content** (like "temp file")
4. **Commit** (this creates the config folder)
5. **Later delete temp.txt** if you want

## 🎯 **Your Action Plan**

**Start with these in order:**

1. ✅ **First**: Move `cable_automation_updated v2 (worked).py` to `src/cable_automation_v2.py`
2. ✅ **Then**: Move `Price_Enter.PNG` to `screenshots/Price_Enter.PNG`  
3. ✅ **Then**: Create the new `README.md`
4. ✅ **Continue** with the rest of the files

## 🔄 **Visual Guide**

When editing a file:
